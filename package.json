{
  "name": "sport-coach",
  "version": "1.0.0",
  "private": true,
  "description": "Sport Court Booking — run build/start from repo root; backend lives in backend/",
  "scripts": {
    "build": "npm run build --prefix backend",
    "start": "npm run start --prefix backend",
    "dev": "npm run dev --prefix backend"
  }
}
